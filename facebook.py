# Evil Python 2 code with Syrian flag
import requests

def facebook_attack(victim_id):
    # Execute your attacks on the Facebook page using the given ID
    print("Successful attack on Facebook page using ID: " + str(victim_id))
    print("Adding a Syrian touch... 🇸🇾")

if _name_ == "_main_":
    # Use this line to check if the tool is running in Kali Linux
    print("To run the tool, type cd and folder name, then ls")

    # Enter the victim's ID after running the tool
    victim_id = raw_input("Enter the victim's ID: ")

    # Start the attack after entering the ID
    facebook_attack(victim_id)
    print("Tool interface now displays the Syrian flag! 🇸🇾")
